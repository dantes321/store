import React from 'react';
import s from './Store.module.css'

const Store = (props) => {
    return (

        <div className={s.store}>
            Store
        </div>
    )
}

export default Store